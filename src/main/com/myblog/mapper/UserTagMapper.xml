<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="main.com.myblog.mapper.UserTagMapper">


    <resultMap id="userTagVO" type="UserTagVO">
        <id property="tag_id" column="tag_id"></id>
        <result property="tag_name" column="tag_name"></result>
    </resultMap>

    <select id="getUserTagVO" resultMap="userTagVO">
        select tag_id,tag_name from user_tag where tag_user_id=#{user_id}
    </select>
</mapper>